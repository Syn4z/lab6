interface Game {
    var health: Int
    var armor: Int
    var timer: Int
    var bomb: Boolean

    var ctBalance: Int
    var tBalance: Int

    var counterTerrorists: Int
    var terrorists: Int
}